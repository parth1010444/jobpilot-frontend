# JobPilot Spring Boot API origin (no trailing slash).
# Local default matches backend bootRun on :8080.
# Production: set this at build time to the public API URL the browser will call.
VITE_API_BASE_URL=http://localhost:8080
